const createDeck = (deck) => {
    return deck
  }
  
  const countCards = (deck) => {
    let deckLength = deck.length
    return deckLength
  }
  
  module.exports = {
    createDeck,
    countCards,
  }